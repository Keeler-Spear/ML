public class NeuralNetwork {

    //Depth = number of hiden layers
    //width = number of nodes per hidden layer
    public NeuralNetwork(int depth, int[] width, String activationFunction, String objectiveFunction) {}

    public Matrix predictPrivate(Matrix x) {
        return LinearAlgebra.randMatrix(1,1, -1, 1);
    }

    public double predict(Matrix x) {
        return 0.0;
    }

}
